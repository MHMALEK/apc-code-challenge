import React from 'react';
import { FontProviderProps } from './types';

const FontProvider: React.FunctionComponent<FontProviderProps> = ({
  children,
}) => <>{children}</>;

export default FontProvider;
